{
  "name": "secretary-gas",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "push": "npm run typecheck && vite build && clasp push"
  },
  "devDependencies": {
    "@gas-plugin/unplugin": "^0.1.2",
    "@types/google-apps-script": "^1.0.0",
    "typescript": "^5.6.0",
    "vite": "^7.0.0"
  }
}
